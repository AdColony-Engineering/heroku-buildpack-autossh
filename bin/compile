#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

compile() {
  local -r script_dir="$(dirname "${BASH_SOURCE[0]}")"
  "${script_dir}/install-autossh" "$@"
  "${script_dir}/start-autossh" "$@"
}
compile "$@"
